<resources>

    <!-- Base application theme using Material 3. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight">
        <!-- Brand colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- ActionBar styling -->
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="android:actionBarTheme">@style/AppTheme.ActionBarTheme</item>
        <item name="actionBarTheme">@style/AppTheme.ActionBarTheme</item>

        <!-- Force title color at the theme level -->
        <item name="android:titleTextColor">@android:color/white</item>
        <item name="titleTextColor">@android:color/white</item>

        <!-- CRITICAL: Define text colors based on the theme (Black in Light, White in Dark) -->
        <item name="android:textColorPrimary">?attr/colorOnSurface</item>
        <item name="android:textColorSecondary">?attr/colorOnSurfaceVariant</item>

        <!-- Standard preference and dialog themes -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="alertDialogTheme">@style/ThemeOverlay.Material3.MaterialAlertDialog</item>
    </style>

    <!-- Transparent system bars for edge-to-edge activities. -->
    <style name="AppTheme.TransparentSystemBars" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Branded ActionBar Style -->
    <style name="AppTheme.ActionBar" parent="Widget.Material3.ActionBar.Solid">
        <item name="android:background">@color/colorPrimary</item>
        <item name="background">@color/colorPrimary</item>
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.TitleTextStyle</item>
        <item name="titleTextStyle">@style/AppTheme.ActionBar.TitleTextStyle</item>
    </style>

    <!-- Explicit White Title Style -->
    <style name="AppTheme.ActionBar.TitleTextStyle" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Force Dark Overlay for the ActionBar -->
    <style name="AppTheme.ActionBarTheme" parent="ThemeOverlay.Material3.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:titleTextColor">@android:color/white</item>
        <item name="titleTextColor">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <!-- Legacy style name kept for compatibility -->
    <style name="AppTheme.AlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog" />

    <!-- Style for text in tabs -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>
    </style>

</resources>
