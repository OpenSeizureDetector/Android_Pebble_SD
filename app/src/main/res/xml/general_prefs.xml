<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="darkMode"
        android:title="App Theme"
        android:summary="Choose between light, dark, or system default theme"
        android:entries="@array/dark_mode_entries"
        android:entryValues="@array/dark_mode_values"
        android:defaultValue="system"
        android:dialogTitle="Select Theme" />


    <CheckBoxPreference
        android:defaultValue="false"
        android:key="AutoStart"
        android:summary="@string/auto_start_summary"
        android:title="@string/auto_start_title"
        android:enabled="true"
        />

    <CheckBoxPreference
        android:defaultValue="false"
        android:key="PreventBatteryOptWarning"
        android:summary="@string/prevent_bat_opt_summary"
        android:title="@string/prevent_bat_opt_title"
        android:enabled="true"
        />

    <CheckBoxPreference
        android:defaultValue="false"
        android:key="first_run_complete"
        android:summary="Prevent the welcome/setup screens displaying when the app starts (they are not needed after initial set-up)."
        android:title="Disable Welcome Screens"
        android:enabled="true"
        />
    <EditTextPreference
        android:defaultValue=""
        android:key="AppVersionName"
        android:summary="@string/app_version_summary"
        android:title="@string/app_version_title" />


</PreferenceScreen>
